{
  "name": "@sovereign/identity",
  "version": "0.1.0",
  "description": "Sovereign Identity — Unified RBAC, Namespace, Audit, Keys, VC, XSMP",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
